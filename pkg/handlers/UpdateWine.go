package handlers

import "net/http"

func UpdateWine(w http.ResponseWriter, r *http.Request) {
	// read dynamic id
	//read req body
	// iterates over wines
	// update and send res back when id matches
}
